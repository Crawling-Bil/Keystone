# Pinned, fully-resolved dependency versions for Keystone (Network Engineer
# Suite Web). Generated from requirements.txt's version ranges on 2026-09-06.
#
# requirements.txt lists the direct, range-pinned dependencies this app is
# written against; this file pins every one of them PLUS their transitive
# dependencies to exact versions, so `pip install -r requirements-lock.txt`
# reproduces the same environment every time instead of silently picking up
# a newer minor/patch release. Regenerate after deliberately updating a
# dependency:
#   python3 -m venv /tmp/lockenv && source /tmp/lockenv/bin/activate
#   pip install -r requirements.txt && pip freeze > requirements-lock.txt
# (then remove this header and re-add it).

bcrypt==5.0.0
blinker==1.9.0
cffi==2.1.1
click==8.5.0
cryptography==50.0.1
et_xmlfile==2.0.0
Flask==3.1.3
invoke==3.0.3
itsdangerous==2.2.0
Jinja2==3.1.6
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
netmiko==4.7.0
ntc_templates==9.2.0
openpyxl==3.1.5
paramiko==4.0.0
psutil==7.2.2
pycparser==3.0
pyftpdlib==2.2.0
Pygments==2.21.0
PyNaCl==1.6.2
pyserial==3.5
PyYAML==6.0.3
rich==15.0.0
ruamel.yaml==0.19.1
scp==0.16.1
textfsm==2.1.0
typing_extensions==4.16.0
waitress==3.0.2
Werkzeug==3.1.8
